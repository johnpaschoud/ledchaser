// Uses Adafruit Neopixel driver extension. Connect Din of strip to ZiP pin of Kitronik Create motor board (is u:bit pin 1).
let strip = neopixel.create(DigitalPin.P0, 32, NeoPixelMode.RGB)
basic.forever(function () {
	
})
